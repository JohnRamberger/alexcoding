package part1;
public class b {
  public static void main(String[] args) {
    // Write a function/method to add two numbers and then take square the sum (must use 2 parameters)
 
 
 
 //Alex
  int one = 1;
  int two = 2;



System.out.println(addthensquare(one, two));
 
 
  }

  // curly brackets = scope
static int addthensquare(int one, int two) {
   int sum = (one + two);
   int product = (sum * sum);
   return (product);
 
}

// class > function > line
















//Q: int in parameter but not in sum =""?
//addthensquare(one, two);

double a = 2;
 double b = 3;
static double f(double a, double b){
double absumsquared = (a+b)^2;
return absumsquared;
 }
